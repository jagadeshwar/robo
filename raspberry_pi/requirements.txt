flask>=3.0
flask-socketio>=5.3
pyserial>=3.5
pillow>=10.0
numpy>=1.24
# tflite-runtime was removed from PyPI — install separately on the Pi:
#   pip install tflite-runtime --extra-index-url https://google-coral.github.io/py-repo/
# Or use full TensorFlow (slower install):
#   pip install tensorflow
